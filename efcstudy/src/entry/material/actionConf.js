/**
 * @file 模块 `entry/material` - Action配置
 *
 * @author liuming07(liuming07@baidu.com)
 * @date 2015-4-3
 */

define(function (require) {
    var confList = [
        {
            path: '/entry/material',
            type: 'entry/material/Action'
        }
    ];

    return confList;
});
